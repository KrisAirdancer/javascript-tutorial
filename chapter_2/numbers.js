let radius = 10;
const pi = 3.14;

console.log(radius, pi);

console.log(10 / 2);

// Modulus operator. Returns the remainder of the left item divided by the right.
let remainder = radius % 3;
console.log(remainder);

let clicks = 10;

clicks++; // Shorthand. Also works for subtraction.
console.log(clicks);

clicks += radius; // Shorthand. Also works for subtraction, multiplication, and division.
console.log(clicks);

clicks /= radius;
console.log(clicks);

let concatenation  = 'The webpage got ' + clicks + ' yesterday.';
console.log(concatenation);


console.log('***** END numbers *****');